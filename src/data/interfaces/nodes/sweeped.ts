import { Transform } from '../properties/transform';

export class Sweeped {
  shapeClass: "sweep";
  name?: string;
  mesh?: any;
  transform?: Transform;
  profile?: any;
  path?: any;
  round?: boolean;
  shape?: any;
}